A bash script for the initial setup and hardening of a fresh Ubuntu VPS
 
### Requirements
 
- A fresh Ubuntu VPS
- Root access
- Your SSH public key
### Usage
 
Edit the `CONFIG` section at the top of the script, then run it as root:
 
```bash
sudo bash vps-setup.sh
```
